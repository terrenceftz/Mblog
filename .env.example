# compose 对外端口
PORT=80

# 必填（compose 阶段缺失即报错，不会回退弱默认值）：
# JWT_SECRET：至少 32 位随机字符串，可用 `openssl rand -hex 32` 生成
JWT_SECRET=
# ADMIN_PASSWORD：至少 8 位；生产环境首次启动会用它创建管理员账号
ADMIN_PASSWORD=
# 管理员用户名（可选，默认 admin）
ADMIN_USERNAME=admin
